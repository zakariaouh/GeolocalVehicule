package ma.vinci.geolocalvehicule.action;

import java.util.ArrayList;


import ma.vinci.geolocalvehicule.modele.User;
import ma.vinci.geolocalvehicule.modele.Vehicule;

public class VehiculetActions {// a supprimer
	
	/*
	//public static ArrayList<Vehicule> vehiculelist;
public static ArrayList<Vehicule>getvihicules(){
	//data from webservice
	ArrayList<Vehicule> array =new ArrayList<Vehicule>();
	   for (int i = 0; i < 5; i++) {
		   User driver=new User("Driver"+i,"Password");
		  Vehicule vhcl= new Vehicule(driver, 0.0, 0.0, 0, "String", i%3,"","description"+i*7);
		   array.add(vhcl);
		  // vehiculelist.add(new Vehicule(new User("Driver"+i,"Password"), 0.0, 0.0, 0, "String", i%3,"","description"+i*7));
		   
		}
  
	return array;
	
}/**/
/*public static Vehicule getvihiculeinfo(int position){
	return vehiculelist.get(position);
}*/
}
